const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.lotDetails.findMany({ where: { projectId: 'cmlzyvrem0004v9yg4n7oy70o' }, include: { mapElement: true } }).then(lots => { console.log('LOTS COUNT:', lots.length); lots.slice(0, 1).forEach(l => console.log(JSON.stringify(l, null, 2))) }).catch(e => console.error(e.message)).finally(() => prisma.$disconnect())
